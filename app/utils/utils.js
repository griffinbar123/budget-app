export const formatter = new Intl.NumberFormat('en-US', {
    style: 'percent',
    minimumFractionDigits: 2, // Optional: Set minimum decimal places
    maximumFractionDigits: 2 // Optional: Set maximum decimal places
  });